model:
  name: m2
  base_model: codesage_v2
dataset:
  name: d3
  pr_data: true
  commit_data: true
  model: qwen_32b
  dataset_name: commit_plus_PR_qwen_32B
hyperparam:
  epochs: 1
  batch_size: 1
  lr: 2.0e-05
  logging:
    log_level: INFO
    save_model: true
experiment:
  name: ${model.name}_${dataset.name}
